<?php

// Add Shortcode to display 12 column grid
function sc_gs_12( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-12">' . $content . '</div>';
}
add_shortcode( 'sc_gs_12', 'sc_gs_12' );

// Add Shortcode to display 11 column grid
function sc_gs_11( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-11">' . $content . '</div>';
}
add_shortcode( 'sc_gs_11', 'sc_gs_11' );

// Add Shortcode to display 10 column grid
function sc_gs_10( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-12">' . $content . '</div>';
}
add_shortcode( 'sc_gs_10', 'sc_gs_10' );

// Add Shortcode to display 9 column grid
function sc_gs_9( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-9">' . $content . '</div>';
}
add_shortcode( 'sc_gs_9', 'sc_gs_9' );

// Add Shortcode to display 8 column grid
function sc_gs_8( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-8">' . $content . '</div>';
}
add_shortcode( 'sc_gs_8', 'sc_gs_8' );

// Add Shortcode to display 7 column grid
function sc_gs_7( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-7">' . $content . '</div>';
}
add_shortcode( 'sc_gs_7', 'sc_gs_7' );

// Add Shortcode to display 6 column grid
function sc_gs_6( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-6">' . $content . '</div>';
}
add_shortcode( 'sc_gs_6', 'sc_gs_6' );

// Add Shortcode to display 5 column grid
function sc_gs_5( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-5">' . $content . '</div>';
}
add_shortcode( 'sc_gs_5', 'sc_gs_5' );

// Add Shortcode to display 4 column grid
function sc_gs_4( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-4">' . $content . '</div>';
}
add_shortcode( 'sc_gs_4', 'sc_gs_4' );

// Add Shortcode to display 3 column grid
function sc_gs_3( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-3">' . $content . '</div>';
}
add_shortcode( 'sc_gs_3', 'sc_gs_3' );

// Add Shortcode to display 2 column grid
function sc_gs_2( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-2">' . $content . '</div>';
}
add_shortcode( 'sc_gs_2', 'sc_gs_2' );

// Add Shortcode to display 1 column grid
function sc_gs_1( $atts, $content = null ) {
	return '<div class="sc-gs-col sc-gs-1">' . $content . '</div>';
}
add_shortcode( 'sc_gs_1', 'sc_gs_1' );

